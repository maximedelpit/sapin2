<%= form_tag finalisation_report_path(@report) do %>
  <% @report_obligations.each do |ro| %>
    <h2><%= ro.obligation.title %></h2>
    <% ro.obligation.tasks.each do |d| %>
      <h4><%= d.title %></h4>

      <%= simple_fields_for 'rods[]',
          d.report_obligation_tasks.build(report_obligation: ro) do |f| %>
        <%= f.label :status %>
        <%= f.select :status, enum_options_for_select(ReportObligationTask, :status) %>
        <%= f.hidden_field :task_id %>
        <%= f.hidden_field :report_obligation_id %>
      <% end %>

    <% end %>

  <% end %>
  <%= hidden_field_tag :go_to, go_to %>
  <%= submit_tag %>
<% end %>
